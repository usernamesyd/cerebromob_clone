import 'package:flutter/material.dart';


class RequestRecordPage extends StatelessWidget {
  @override
Widget build(BuildContext context) {
    return Center(
      child: Text('Request and Record Page'),
    );
  }
}